{
  "name": "webgen",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist && mkdir dist",
    "prepare": "npm run build",
    "postinstall": "node scripts/setup.js"
  },
  "bin": {
    "webgen": "./lib/index.js"
  },
  "files": [
    "lib",
    "src",
    "static",
    "styles"
  ],
  "dependencies": {},
  "devDependencies": {}
}
